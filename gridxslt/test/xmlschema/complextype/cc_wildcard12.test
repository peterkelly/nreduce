Complex type attribute wildcard of a derived type. For <extension>, if both the
derived type and the base type have attribute wildcards, then the union
of the two are taken. It is an error if this union is not expressible.

Note: This is not explicitly stated by the spec, however the similar case of
the intersection with attribute group wildcards is in 3.4.3-1-4, so it seems
sensible to do the same for union.

=================================== PROGRAM ====================================
testxmlschema -w
==================================== INPUT =====================================
<?xml version="1.0"?>
<xsd:schema xmlns:xsd="http://www.w3.org/2001/XMLSchema" targetNamespace="http://target.org"
            xmlns:tns="http://target.org">
  <xsd:complexType name="ct1">
    <xsd:complexContent>
      <xsd:extension base="tns:ct2">
        <xsd:sequence/>
        <xsd:anyAttribute namespace="##other"
                          processContents="lax"/>
      </xsd:extension>
    </xsd:complexContent>
  </xsd:complexType>
  <xsd:complexType name="ct2">
    <xsd:anyAttribute namespace="http://ns4.org http://ns2.org ##local"
                      processContents="skip"/>
  </xsd:complexType>
</xsd:schema>
==================================== OUTPUT ====================================
runtests.tmp/input.xml:4: error: Union of local attribute wildcard and base type's attribute wildcard is not expressible, because one is a negation and the other is a set of namespaces including ##local but not ##targetNamespace (XML Schema Part 2: Datatypes Second Edition, section 3.10.6)
================================== RETURN CODE =================================
1
