Tests that the graph replacement algorithm handles arrays correctly, where the
part of the graph to be replaced is in the tail of the array
=================================== PROGRAM ====================================
nreduce -a test runtests.tmp/test.l
===================================== FILE =====================================
test.l
test a b c d e = (letrec
                    x = (if a b c)
                  in
                    (if x
                        (testarray a (cons x a))
                        (testarray a 32)))

main = nil
==================================== OUTPUT ====================================
test a b c d e = (letrec 
                    _v0 = (cons a (cons a (cons a 32)))
                  in
                    (if a 
                        (if b 
                            (cons a (cons a (cons a (cons b a)))) 
                            _v0)
                        (if c 
                            (cons a (cons a (cons a (cons c a)))) 
                            _v0)))
================================== RETURN CODE =================================
0
