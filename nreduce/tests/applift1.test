=================================== PROGRAM ====================================
nreduce -o runtests.tmp/test.l
===================================== FILE =====================================
test.l
(foo (f)
  (+ (let ((a 1)
           (b 2))
          (f (let ((c (* a 2))
                   (d (* b 2)))
                  (+ c d))
             b))
     3))

(main () nil)
==================================== OUTPUT ====================================
foo f = + (let ((a 1)
                (b 2))
               (foo#1 b a f))
          3
main = nil
foo#1 b a f = f (let ((d (* b 2))
                      (c (* a 2)))
                     (+ c d))
                b
================================== RETURN CODE =================================
0
