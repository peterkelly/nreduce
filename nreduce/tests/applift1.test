=================================== PROGRAM ====================================
nreduce -n -o runtests.tmp/test.l
===================================== FILE =====================================
test.l
foo f =
  (+ (letrec 
        a = 1
        b = 2
      in
        (f (letrec
              c = (* a 2)
              d = (* b 2)
            in
              (+ c d))
             b))
     3)

main = nil
==================================== OUTPUT ====================================
foo f = (+ (letrec 
              a = 1
              b = 2
            in
              (foo#1 b a f))
           3)
main = nil
foo#1 b a f = (f (letrec 
                    d = (* b 2)
                    c = (* a 2)
                  in
                    (+ c d))
                 b)
================================== RETURN CODE =================================
0
