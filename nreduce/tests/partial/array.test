=================================== PROGRAM ====================================
nreduce -a genarr runtests.tmp/test.l
===================================== FILE =====================================
test.l
tail6 lst = (tail (tail (tail (tail (tail (tail lst))))))

genarr x =
(letrec
   f = (if x 369 (* 400 2))
   lst = (cons 365 (cons 366 (cons 367 (cons 368 (cons f (cons 370 nil))))))
in
  (if (tail6 lst) lst lst))

main = nil
==================================== OUTPUT ====================================
genarr x = (cons 365 (cons 366 (cons 367 (cons 368 (cons (if x 
                                                 369 
                                                 800)
                                             (cons 370 nil))))))
================================== RETURN CODE =================================
0
