Tests that the graph replacement algorithm handles arrays correctly, where the
part of the graph to be replaced is in one of the elements of the array
=================================== PROGRAM ====================================
nreduce -a test runtests.tmp/test.l
===================================== FILE =====================================
test.l
test a b c d e = (letrec
                    x = (if a b c)
                  in
                    (if x
                        (testarray x (cons a a))
                        (testarray a 32)))

main = nil
==================================== OUTPUT ====================================
test a b c d e = (letrec 
                    _v0 = (cons a (cons a (cons a 32)))
                    _v1 = (cons a a)
                  in
                    (if a 
                        (if b 
                            (cons b (cons b (cons b _v1))) 
                            _v0)
                        (if c 
                            (cons c (cons c (cons c _v1))) 
                            _v0)))
================================== RETURN CODE =================================
0
